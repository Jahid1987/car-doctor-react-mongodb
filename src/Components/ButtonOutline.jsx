const ButtonOutline = ({ children }) => {
  return (
    <button className="btn btn-outline btn-sm md:btn-md btn-error">
      {children}
    </button>
  );
};

export default ButtonOutline;
